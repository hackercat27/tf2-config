exec config_manual

alias delay "wait 5"

alias single "+reload;delay;-reload;"
alias double "single;single"

alias +mod_medic_vaccinator "bind r double"
alias -mod_medic_vaccinator "bind r single"
